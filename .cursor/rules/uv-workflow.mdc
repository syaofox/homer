---
description: 使用 uv 管理虚拟环境，uv run 进行开发与测试
alwaysApply: true
---

# uv 开发与测试工作流

本项目使用 [uv](https://docs.astral.sh/uv/) 管理 Python 虚拟环境与依赖。AI 在建议或执行 Python 相关命令时，应遵循以下约定。

## 虚拟环境管理

- **使用 uv 创建/同步环境**：不手动创建 venv，由 uv 管理
- **安装依赖**：使用 `uv add <package>` 或 `uv sync`，不要使用 `pip install`
- **同步环境**：`uv sync` 根据 `pyproject.toml` 同步依赖

## 开发与测试命令

- **运行脚本/应用**：使用 `uv run python ...` 或 `uv run <module>`
- **运行测试**：使用 `uv run pytest` 或 `uv run python -m pytest`
- **运行 Flask 应用**：`uv run flask run` 或 `uv run python -m flask run`
- **执行任意 Python 命令**：统一使用 `uv run` 前缀，确保在项目环境中执行

## 示例

```bash
# ✅ 正确：使用 uv run 运行
uv run python app/main.py
uv run pytest
uv run flask run

# ❌ 错误：直接调用 python（可能使用系统或错误环境）
python app/main.py
pytest
```

## 其他 uv 命令

- `uv lock`：更新锁文件
- `uv add <package>`：添加依赖
- `uv remove <package>`：移除依赖
